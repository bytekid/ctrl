INCLUDE ints ;

DECLARE
  allocate   : Int * Int => (Array Int Int)            ;
  size       : (Array Int Int) => Int                  ;
  select     : (Array Int Int) * Int => Int            ;
  store      : (Array Int Int) * Int * Int => (Array Int Int) ;
  !ARRAY!Int : (Array Int Int)                         ;

